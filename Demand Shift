<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Demand Curve Activity</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    body {
      font-family: sans-serif;
      margin: 2rem;
    }
    .container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
      align-items: start;
    }
    .chart-wrapper {
      width: 100%;
      max-width: 500px;
      height: 400px;
    }
    canvas {
      background-color: #f4f4f4;
      width: 100% !important;
      height: 100% !important;
    }
    .input-section, .questions {
      margin-top: 2rem;
    }
    label {
      display: block;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>
  <h1>Interactive Demand Curve Activity</h1>

  <p>Use the sliders below to change the price of the good and external conditions. Observe how quantity demanded changes and whether the demand curve shifts or not.</p>

  <div class="container">
    <div>
      <h2>Demand Curve</h2>
      <div class="chart-wrapper">
        <canvas id="demandChart"></canvas>
      </div>
    </div>
    <div class="input-section">
      <label for="priceSlider"><strong>Price:</strong> <span id="priceVal">10</span></label>
      <input type="range" id="priceSlider" min="1" max="20" value="10">

      <label for="incomeChange"><strong>Consumer Income:</strong> <span id="incomeVal">0%</span></label>
      <input type="range" id="incomeChange" min="-50" max="50" value="0">
    </div>
  </div>

  <div class="questions">
    <h2>Reflection Questions</h2>
    <ol>
      <li>What happens to quantity demanded when the price increases or decreases?</li>
      <li>What happens to the demand curve when consumer income increases?</li>
      <li>Which changes represent a <em>movement along</em> the demand curve?</li>
      <li>Which changes represent a <em>shift</em> of the demand curve? Why?</li>
    </ol>
  </div>

  <script>
    const ctx = document.getElementById('demandChart').getContext('2d');

    function getDemandData(incomePercent) {
      const baseIntercept = 20; // baseline quantity when price = 0
      const slope = 1;
      const incomeShift = incomePercent / 5;
      const intercept = baseIntercept + incomeShift;

      return Array.from({ length: 20 }, (_, i) => {
        const Q = i + 1;
        const P = Math.max(0, intercept - slope * Q);
        return { x: Q, y: P };
      });
    }

    const priceSlider = document.getElementById('priceSlider');
    const incomeSlider = document.getElementById('incomeChange');
    const priceVal = document.getElementById('priceVal');
    const incomeVal = document.getElementById('incomeVal');

    const chart = new Chart(ctx, {
      type: 'scatter',
      data: {
        datasets: [{
          label: 'Demand Curve',
          data: getDemandData(0),
          showLine: true,
          borderColor: 'blue',
          backgroundColor: 'blue',
          fill: false
        }, {
          label: 'Current Point',
          data: [{ x: 10, y: 10 }],
          backgroundColor: 'red',
          pointRadius: 6,
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        scales: {
          x: {
            type: 'linear',
            title: { display: true, text: 'Quantity' },
            min: 0,
            max: 20
          },
          y: {
            title: { display: true, text: 'Price' },
            min: 0,
            max: 20
          }
        }
      }
    });

    function updateChart() {
      const price = parseInt(priceSlider.value);
      const income = parseInt(incomeSlider.value);
      priceVal.textContent = price;
      incomeVal.textContent = `${income}%`;

      const intercept = 20 + income / 5;
      const quantity = Math.max(0, intercept - price);

      chart.data.datasets[0].data = getDemandData(income);
      chart.data.datasets[1].data = [{ x: quantity, y: price }];
      chart.update();
    }

    priceSlider.addEventListener('input', updateChart);
    incomeSlider.addEventListener('input', updateChart);
  </script>
</body>
</html>
